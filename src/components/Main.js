import styled from 'styled-components';

const Main = (props) => {

    return (
        <Cointainer>
            Main
        </Cointainer>

    );
}

const Cointainer = styled.div`
    grid-area: main;
`;

export default Main;
